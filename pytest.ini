[pytest]
markers =
    slow: long-running walk-forward/backtest suites (minutes, not seconds).
        Run with `pytest -m "not slow"` for the fast suite;
        `pytest` (no filter) runs everything.
